import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    environment: 'node',
    globals: true,
    // عزل كل ملف اختبار في بيئة منفصلة.
    //
    // السبب: ثلاثة ملفات (codes / security / redemption-atomic) تشغّل Miniflare
    // داخل نفس العملية وتتشارك حالة عامة، فيمنع آخر ملف يُحمَّل غيره من استبدالها.
    // النتيجة إخفاقات **وهمية** تختفي عند تشغيل الملف منفرداً — أي أنها تُخفي
    // عيوباً حقيقية وتُضيّع وقت التشخيص. العزل يجعل النتيجة قابلة للاعتماد.
    //
    // التكلفة: أبطأ قليلاً — وهي مقبولة مقابل موثوقية الإشارة.
    isolate: true,
  },
});
